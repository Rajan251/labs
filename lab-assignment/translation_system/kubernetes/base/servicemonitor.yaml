apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: translation-system-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: translation-worker
  endpoints:
  - port: metrics
    interval: 15s
    path: /metrics
