#!/bin/bash
# Backup script for Kubernetes resources

echo "Starting backup..."
# Example: Backup all resources in all namespaces
# kubectl get all --all-namespaces -o yaml > cluster-backup.yaml

echo "Backup complete."
