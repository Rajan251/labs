# Performance Tests
# Load testing configuration (e.g., k6, locust)

apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-script
data:
  test.js: |
    import http from 'k6/http';
    import { sleep } from 'k6';
    export default function () {
      http.get('http://frontend-service');
      sleep(1);
    }
